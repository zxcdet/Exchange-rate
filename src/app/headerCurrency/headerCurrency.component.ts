import { ChangeDetectionStrategy, Component, Input } from '@angular/core';import { ExchangeRateInterface } from '../types/exchangeRate.interface';@Component({  selector: 'app-header',  templateUrl: './headerCurrency.component.html',  styleUrls: ['./headerCurrency.component.css'],  changeDetection: ChangeDetectionStrategy.OnPush})export class HeaderCurrencyComponent {  @Input() set currencyList(curency: ExchangeRateInterface[]) {    this.USD = curency.find((value) => value.cc === 'USD').rate;    this.EUR = curency.find((value) => value.cc === 'EUR').rate;  }  public USD!: number | undefined  public EUR!: number | undefined}
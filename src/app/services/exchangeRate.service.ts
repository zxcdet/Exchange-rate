import { Injectable } from '@angular/core';import { HttpClient } from '@angular/common/http';import { Observable, tap } from 'rxjs';import { environment } from '../../environments/environment';import { ExchangeRateInterface } from '../types/exchangeRate.interface';@Injectable()export class ExchangeRateService {  constructor(private http: HttpClient) {  }  getRate(): Observable<ExchangeRateInterface[]> {    const url = `${environment.apiUrl}/NBUStatService/v1/statdirectory/exchange?json`    return this.http.get<ExchangeRateInterface[]>(url)      .pipe(tap((value) => value.unshift({        exchangedate: "",        r030: 0,        txt: "",        rate: 1,        cc: "UAN"      })))  };}